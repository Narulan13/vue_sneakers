<script setup>
  const emit = defineEmits(['openDrawer'])

  defineProps({
    totalPrice: Number
  })

</script>



<template>
      <header class="flex justify-between border-b border-slate-100 px-10 py-8">
        <router-link to="/">
          <div class="flex items-center gap-4">
            <img src="/logo.png" alt="logo" class="w-10">
            <div>
              <h2 class="text-xl font-bold">VUE SNEAKERS</h2>
              <p class="text-slate-400">Магазин лучших кроссовок</p>
            </div>
        </div>
        </router-link>
        <ul class="flex items-center gap-10">
          <li @click="() => emit('openDrawer')" class="flex items-center gap-2 cursor-pointer text-gray-500 hover:text-black transition duration-300">
            <img src="/cart.svg" alt="cart">
            <b>{{ totalPrice }} руб.</b>
          </li>
          <router-link to="/favorites">
            <li router-link="/favorites" class="flex items-center gap-2 cursor-pointer text-gray-500 hover:text-black transition duration-300">
              <img src="/heart.svg" alt="cart">
              <span>Закладки</span>
            </li>
          </router-link>
          <li class="flex items-center gap-3 cursor-pointer text-gray-500 hover:text-black transition duration-300">
            <img src="/profile.svg" alt="cart">
            <span>Профиль</span>
          </li>
        </ul>
      </header>
  </template>
  